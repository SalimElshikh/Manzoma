@model IEnumerable<ElecWarSystem.Models.Mot8ayeratEst3dadQetali>
<link href="~/Content/Mot8ayeratEst3dadQetali.css" rel="stylesheet" />
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_NewTamam.cshtml";
}

<h2 class="text-center text-lg-center fs-1 ">المتغيرات في حالة الاستعداد القتالي </h2>
<p>
    @Html.ActionLink("تسجيل", "Create", "", new { @class = "save-btn text-decoration-none p-3" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.A8radTa7arok.a8radTa7arok)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Al7ala.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ge7aTasdek.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Item)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateFrom)
        </th>
        <th> الحدث</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.A8radTa7arok.a8radTa7arok)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Al7ala.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ge7aTasdek.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Item)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateFrom)
            </td>
            <td >

                @*@Html.ActionLink("حذف", "Delete", new { id = item.ID, @class = " " })*@
                <a href="@Url.Action("Delete", new { id = item.ID })" class="btn"><i class="fa-regular fa-trash-can"></i></a>

            </td>
        </tr>
    }

</table>
